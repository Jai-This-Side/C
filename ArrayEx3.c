#include <stdio.h>
int main(int argc, char const *argv[])
{
    int i;
    int array[i];

    for (int i = 0; i < 10; i++)
    {
        printf("enter the number in %d position of array\n", i);
        scanf("%d", &array[i]);
    }

    
    
    return 0;
}
