//take 10 characters input from user and count the number of special characters, numeric, capital and small alphabets from an array.

#include <stdio.h>
int main(int argc, char const *argv[])
{
    int arr1[i]
    return 0;
}
